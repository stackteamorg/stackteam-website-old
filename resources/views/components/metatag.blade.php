<meta @if($name != null)name="{{ $name }}"@endif
@if($charset != null) charset="{{ $charset }}"@endif
@if($property != null) property="{{ $property }}"@endif
@if($content !=null) content="{{ $content }}"@endif />